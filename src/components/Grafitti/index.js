import React from "react"
import styled from "styled-components/macro"

export default styled.p`
  color: hsl(57, 80%, 41%);
  text-shadow: 1px 11px 7px #b5b1b1;
  mix-blend-mode: color;
  filter: contrast(1.3) brightness(2);
`
